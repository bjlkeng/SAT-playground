// STUB — full port of src/tiers.c (kissat 4.0.4) lands with the core wave.

use crate::internal::Solver;

pub fn print_tier_usage_statistics(solver: &mut Solver, _stable: bool) {
    let _ = solver;
}
